// Superseded by SocialIcon.tsx (component) + ../data/socialLinks.ts (data),
// split apart to satisfy the react-refresh/only-export-components lint rule.
// Kept as a re-export so any stale imports of this path still resolve.
export { default } from './SocialIcon'
