apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: taskflow-backend
  labels:
    app: backend
spec:
  type: LoadBalancer
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  sessionAffinity: None
